apiVersion: v1
kind: Service
metadata:
  name: order-db-service
  namespace: prod
  labels:
    app: order-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: order-db
