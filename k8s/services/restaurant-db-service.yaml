apiVersion: v1
kind: Service
metadata:
  name: restaurant-db-service
  namespace: prod
  labels:
    app: restaurant-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: restaurant-db
