apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: prod
  labels:
    app: payment-service
spec:
  type: ClusterIP
  ports:
    - port: 3004
      targetPort: 3004
      protocol: TCP
  selector:
    app: payment-service
