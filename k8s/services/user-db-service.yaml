apiVersion: v1
kind: Service
metadata:
  name: user-db-service
  namespace: prod
  labels:
    app: user-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: user-db
